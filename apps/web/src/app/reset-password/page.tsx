'use client';
import Link from 'next/link'; import { FormEvent,Suspense,useEffect,useState } from 'react'; import { useSearchParams } from 'next/navigation'; import { PublicAuthCard } from '@/components/auth/public-auth-card'; import { apiRequest } from '@/lib/api';
function Form(){const token=useSearchParams().get('token')??'';const[valid,setValid]=useState<boolean|null>(null);const[password,setPassword]=useState('');const[confirmation,setConfirmation]=useState('');const[error,setError]=useState('');const[success,setSuccess]=useState('');const[loading,setLoading]=useState(false);useEffect(()=>{if(!token){setValid(false);return}apiRequest<{valid:boolean}>(`/auth/validate-reset-token?token=${encodeURIComponent(token)}`,{authenticated:false}).then(r=>setValid(r.valid)).catch(()=>setValid(false))},[token]);async function submit(e:FormEvent){e.preventDefault();setError('');if(password!==confirmation)return setError('A confirmação de senha não corresponde.');setLoading(true);try{const r=await apiRequest<{message:string}>('/auth/reset-password',{method:'POST',authenticated:false,body:{token,newPassword:password}});setSuccess(r.message);setValid(false)}catch(r){setError(r instanceof Error?r.message:'Não foi possível redefinir a senha.')}finally{setLoading(false)}}if(valid===null)return <div className="screen-loader compact"><span className="spinner"/>Validando link...</div>;return <>{error&&<div className="alert error">{error}</div>}{success?<div className="alert success">{success} <Link href="/login">Ir para o login</Link></div>:valid?<form className="public-auth-form" onSubmit={submit}><label>Nova senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} maxLength={72} required autoFocus/></label><small className="password-hint">Use letras, números e caractere especial.</small><label>Confirmar nova senha<input type="password" value={confirmation} onChange={e=>setConfirmation(e.target.value)} minLength={8} required/></label><button className="button primary full" disabled={loading}>{loading?'Redefinindo...':'Redefinir senha'}</button></form>:<div className="alert error">Este link é inválido, expirou ou já foi utilizado. <Link href="/forgot-password">Solicitar outro link</Link></div>}</>}
export default function ResetPasswordPage(){return <PublicAuthCard eyebrow="Nova credencial" title="Redefinir senha" text="Crie uma nova senha segura para acessar o Orbyto."><Suspense fallback={<div className="screen-loader compact"><span className="spinner"/>Carregando...</div>}><Form/></Suspense><p className="auth-back-link"><Link href="/login">Voltar ao login</Link></p></PublicAuthCard>}
