# URL pública da API, incorporada no bundle durante `next build`.
NEXT_PUBLIC_API_URL=https://www.orbyto.com.br/api

# Para desenvolvimento local, use em `.env.local` (não versionado):
# NEXT_PUBLIC_API_URL=http://localhost:3001/api
